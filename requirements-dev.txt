-r requirements.txt

# Test suite. httpx drives FastAPI's ASGI transport directly, so integration
# tests exercise the real app without binding a socket.
pytest==9.0.1
pytest-asyncio==1.3.0
httpx==0.28.1

# Lint / format / types.
ruff==0.15.1
mypy==1.19.0

# Benchmark harness output (docs/SELF_ASSESSMENT.md) and the design PDF.
tabulate==0.9.0
reportlab==4.4.4
